use crate::drawer::TextBlueprint;

pub struct TextRenderer {}

impl TextRenderer {
    pub fn new(dpi: f32) -> Self {
        todo!();
    }

    pub fn draw(&self, viewport: glam::Vec2, dpi: f32, spec: &TextBlueprint<'_>) {
        todo!()
    }
}
